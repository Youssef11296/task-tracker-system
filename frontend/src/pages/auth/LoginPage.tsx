import React from "react";
import { LoginComponent } from "../../components/pagesComponents/authComponents";

const LoginPage = () => {
  return (
    <div>
      <LoginComponent />
    </div>
  );
};

export default LoginPage;
