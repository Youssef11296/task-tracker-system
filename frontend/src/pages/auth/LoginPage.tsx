import React from "react";
import { LoginComponent } from "../../components/authComponents";

const LoginPage = () => {
  return (
    <div>
      <LoginComponent />
    </div>
  );
};

export default LoginPage;
